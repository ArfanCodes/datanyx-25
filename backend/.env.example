# ============================================
# ENVIRONMENT CONFIGURATION
# ============================================

# Environment
NODE_ENV=development
PORT=3000

# ============================================
# DATABASE - SUPABASE POSTGRESQL
# ============================================
# 
# DATABASE_URL: Connection pooler (port 6543) - for application queries
# DIRECT_URL: Direct connection (port 5432) - for migrations only
#
# Get these from: Supabase Dashboard → Settings → Database → Connection string
# - DATABASE_URL: Use "Session" mode
# - DIRECT_URL: Use "Transaction" mode
#
# IMPORTANT: URL-encode special characters in password
# @ → %40, # → %23, & → %26, etc.
# ============================================

# Pooled connection for app queries (Session mode - port 6543)
DATABASE_URL="postgresql://postgres.iwedvgyucyqaghirtxbl:Supabase2025ProjectKey@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres"

# Direct connection for migrations (Transaction mode - port 5432)
DIRECT_URL="postgresql://postgres.iwedvgyucyqaghirtxbl:Supabase2025ProjectKey@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?pgbouncer=true&connection_limit=1"

# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate a secure random string for production:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-at-least-32-characters
JWT_EXPIRES_IN=7d

# ============================================
# SUPABASE (Optional - for Supabase Auth integration)
# ============================================
# Get these from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://iwedvgyucyqaghirtxbl.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# ============================================
# CORS CONFIGURATION
# ============================================
# Comma-separated list of allowed origins
# Local development
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006,http://localhost:3000,exp://192.168.1.1:8081

# ============================================
# OPTIONAL FEATURES
# ============================================
# Email service (if implementing email notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# File upload (if implementing receipt uploads)
AWS_S3_BUCKET=
AWS_ACCESS_KEY=
AWS_SECRET_KEY=

# Redis (if implementing caching)
REDIS_URL=

# Sentry (error tracking)
SENTRY_DSN=
